kind = "web"
previewPath = "/"
title = "Review Radar"
version = "1.0.0"
id = "artifacts/review-radar"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23369

[services.development]
run = "pnpm --filter @workspace/review-radar run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/review-radar", "run", "build" ]
publicDir = "artifacts/review-radar/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23369"
BASE_PATH = "/"
